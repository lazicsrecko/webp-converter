<UserControl x:Class="ImageConverter.View.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ImageConverter.View"
             xmlns:viewmodel="clr-namespace:ImageConverter.ViewModel"
             d:DataContext="{d:DesignInstance Type=viewmodel:AboutViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBlock>About</TextBlock>
        <Button Width="100" Height="50" Command="{Binding GoToHomeCommand}">Go to Home</Button>
    </Grid>
</UserControl>
